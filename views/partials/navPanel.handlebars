{{#if main}}
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">VapeAst</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/">Главное</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/cashier">Режим кассира</a>
                </li>
            </ul>

        </div>
        {{#if cashier}}
        <div class="w-50">
            <select class="form-select w-100" id="seller">
                <option value="Олег">Олег</option>
                <option value="Сула">Сула</option>
                <option value="Клим">Клим</option>
                <option value="Муха">Муха</option>
            </select>
        </div>
        {{/if}}
    </div>
</nav>
{{/if}}